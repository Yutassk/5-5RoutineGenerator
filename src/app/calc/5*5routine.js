let MAX_WEIGHT = 64.5;
let bOnesPlace = MAX_WEIGHT % 10;
let mainWeight = MAX_WEIGHT - bOnesPlace;

let result = 2.5 * Math.floor(bOnesPlace / 2.5);

let inputNum = 0;
let hensuu = 0.55;
let value = inputNum * low;
let multiple = 2.5;
let near = Math.round(value / multiple) * multiple;

console.log;
